<%= form_for(@tourist_destination) do |f| %>
  <% if @tourist_destination.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tourist_destination.errors.count, "error") %> prohibited this tourist_destination from being saved:</h2>

      <ul>
      <% @tourist_destination.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit t('.submit') %>
  </div>
<% end %>
